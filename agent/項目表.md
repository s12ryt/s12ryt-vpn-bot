# 專案檔案與依賴表

| 路徑 | 用途 | 主要依賴／被依賴 |
| --- | --- | --- |
| `agent/question.md` | 已確認需求與驗收契約 | 所有實作與測試依此驗收 |
| `agent/deep_todos.md` | 完整歷史任務 | 交付前更新 |
| `agent/項目表.md` | 檔案、模組與依賴關係 | 新增／移動模組時更新 |
| `agent/memory.md` | 重要操作與驗證紀錄 | 每個 TDD 週期與交付前更新 |
| `go.mod`, `go.sum` | Go module 與鎖定依賴 | 所有 Go 套件 |
| `cmd/server/main.go`, `application.go` | migration、auth/Bot/API、即時與週期資格、VPN 自助核發、核心 worker、流量採集與故障監控的 process 組裝、訊號與 graceful shutdown | `internal/config`、`internal/postgres`、`internal/auth`、`internal/telegram`、`internal/qualification`、`internal/httpapi`、`internal/vpn`、`internal/secrets`、`internal/subscription`、`internal/corecontrol`、`internal/coreworker`、`internal/singbox`、`internal/trafficstats`、`internal/trafficrunner` |
| `cmd/core-controller` | 受限 Unix socket sidecar，固定執行 sing-box check 並只重啟指定容器 | `internal/corecontrol`、sing-box binary、Docker Engine Unix socket |
| `cmd/backup`, `cmd/restore`, `internal/backup` | 每日 PostgreSQL dump、HKDF用途隔離 AES-GCM 封存、原子保留與驗證後還原 | `APP_MASTER_KEY`、PostgreSQL client tools、`backups` volume |
| `internal/config` | bootstrap 環境設定、監聽位址、核心 Unix 路徑與 HKDF 用途隔離金鑰 | server、auth、秘密與核心控制模組使用 |
| `internal/auth` | RBAC、5 分鐘一次性登入碼、12 小時閒置／7 日絕對 Session、登入交換流程 | Telegram command、HTTP API、PostgreSQL store |
| `internal/telegram` | Bot API client、`/adminlogin` 與 `/vpn` handler／每 ID 發碼限速、chat_member 防腐映射與可取消 long poll runner | server、資格事件與 VPN access service |
| `internal/postgres` | auth／資格規則／重查與全域政策／存取狀態／加密憑證／交易式核發 store、加密核心設定、active credential 快照、流量入帳與冪等批次、全域故障封閉、pgx 交易 runner、durable 核心動作 outbox、稽核與版本化 migrations | server 啟動、auth、資格、VPN 核發、Web 管理、流量採集與核心 worker；依賴 pgx |
| `internal/trafficstats`, `internal/trafficrunner` | sing-box V2Ray Stats gRPC 採集、動態 endpoint、嚴格統計解析、0600 原子 spool、優先重播、持久故障狀態協調與管理者通知 | PostgreSQL 共享配額、故障封閉與 server runtime |
| `internal/qualification` | 多群組完整查核、事件觀察值合併、可調節流重試、週期補償重查、未決原因分類、管理者彙總、規則變更 trigger 與 membership application handler | Telegram client／runner、PostgreSQL qualification/access stores、server scheduler |
| `internal/domain/access.go` | 資格、核發、撤銷、輪替與閒置狀態機 | Bot、Web 與持久化服務使用 |
| `internal/domain/qualification.go` | 多群 any/all 三值資格決策 | Telegram 資格查核服務使用 |
| `internal/domain/credentials.go` | 四協定獨立 CSPRNG 憑證與訂閱 token | 核發協調與加密儲存使用 |
| `internal/domain/provisioning.go` | 憑證與帳號核發／批准／輪替協調 | 後續交易式 application service 使用 |
| `internal/domain/quota.go` | 跨協定／雙棧共享配額與固定週期 | 流量匯入與週期排程使用 |
| `internal/domain/traffic_accounting.go` | 配額和最後 VPN 活動的原子記帳 | sing-box 統計採集服務使用 |
| `internal/domain/inactivity.go` | 閒置淘汰受影響使用者預覽 | Web 設定與交易式批次撤銷使用 |
| `internal/secrets` | 用途隔離 AES-256-GCM 憑證加密、通用秘密值加密與 HMAC 訂閱 token lookup digest | PostgreSQL provisioning／credential／core settings stores |
| `internal/singbox` | 四協定單棧／雙棧設定生成、active 快照組裝、同檔案系統原子設定替換與 check/promote/restart/rollback 安裝交易 | PostgreSQL 核心設定／憑證來源、核心 worker 與受限控制 sidecar |
| `internal/corecontrol` | 主應用到 sidecar 的受限 Unix HTTP client／handler，以及固定 binary argv 與固定容器 Docker restart controller | `cmd/core-controller`、`internal/singbox` file deployment |
| `internal/coreworker` | durable outbox 租約動作的安全撤銷優先、計畫預告合併、重啟節流、完成／失敗退避及 Telegram 通知／稽核協調 | PostgreSQL core action／recipient／audit stores、sing-box snapshot／installer、Telegram client |
| `internal/subscription` | 私人 HTTPS 訂閱 URL、User-Agent／query 格式協商，以及 Base64 URI、sing-box JSON、Mihomo 設定輸出 | VPN access service、核心設定／憑證 store 與訂閱 HTTP handler |
| `internal/vpn` | 即時資格查核、交易式自助核發、既有憑證讀取與訂閱連結協調 | Telegram `/vpn` 與 server 組裝 |
| `internal/httpapi` | 健康檢查、auth、可信反代限速、安全 cookie／CSRF、使用者／角色／稽核／全域政策管理與智慧訂閱邊界 | server 入口；依賴 auth、PostgreSQL application stores 與 subscription service 介面 |
| `web/` | React + TypeScript + Vite 管理面板；登入、使用者、管理員角色、稽核、資格／配額／閒置政策工作區 | 呼叫 `/api`；Vitest、ESLint、Vite build 與 Playwright 響應式驗證 |
| `web/go.mod` | 阻止根 Go module 誤掃 `node_modules` 內第三方 Go 範例 | 根層 `go test ./...` 模組邊界 |
| `Dockerfile`, `Dockerfile.controller`, `Dockerfile.backup` | 建置 React/Go 非 root 應用映像、固定核心控制 sidecar 與含 PostgreSQL client tools 的非 root 備份映像 | Node 24、Go 1.26、distroless、PostgreSQL 17、不可變 `SINGBOX_IMAGE` |
| `compose.yaml`, `.env.example`, `deploy/` | host-network VPN 拓撲、loopback PostgreSQL、受限 Docker socket、共享 volume 權限及部署靜態契約測試 | 應用、PostgreSQL、core-controller、sing-box、GitHub Actions Compose 驗證 |
| `.gitignore` | 排除秘密、依賴與建置產物 | Git repository |
| `vpn.md` | 使用者既有空白檔案 | 不修改，除非使用者另行指定 |
